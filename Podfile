# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TableNodeTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Texture'
  pod 'FPSLabel'
  # Pods for TableNodeTest

  target 'TableNodeTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TableNodeTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
